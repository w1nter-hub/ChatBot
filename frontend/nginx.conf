upstream qoldauai_api {
  server qoldauai-api:3000;
}

server {
  listen 80;

  location ~ ^/api(/?)(.*) {
    proxy_pass http://qoldauai_api/$2$is_args$args;
  }

  location / {
    root /usr/share/nginx/html/;
    include /etc/nginx/mime.types;
    try_files $uri $uri/ /index.html;
  }
}
